remove lock = Caps_Lock
keycode 66 = Control_L
add control = Control_L
! because fvwm2 only use C-Tab to switch window, so don't change Tab key to Meta_L
!keycode 127 = Tab
!keycode 23 = Meta_L
!add mod1 = Meta_L
