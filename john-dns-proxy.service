[Unit]
Description= dns proxy
Requires= network.target
After= network.target

[Service]
Type= simple
ExecStart= /usr/bin/python3.4 /root/dns-proxy-server.py server-ip server-udp-prot dns-server-ip
Restart= on-failure
RestartSec= "5min 20s"

[Install]
WantedBy=multi-user.target

